import LoginForm from "../components/LoginForm";
import Logo from "../reusable-ui/Logo";

export const Login = () => {
  return (
    <div className="loginPage">
      <Logo />
      <LoginForm />
    </div>
  );
};
